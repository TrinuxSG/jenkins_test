pipeline {
    agent any
    stages {
        stage('First Step') {
            steps {
                script {
                    def i = currentBuild.changeSets.size()
            echo i
        }
    }
}
                }
            }
