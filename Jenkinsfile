pipeline {
    agent any
    stages {
        stage('First Step') {
            steps {
                script {
            echo Arrays.toString(currentBuild.changeSets.toArray())
        }
    }
}
                }
            }
